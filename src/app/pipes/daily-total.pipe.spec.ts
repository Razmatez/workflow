import { DailyTotalPipe } from './daily-total.pipe';

describe('DailyTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new DailyTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
