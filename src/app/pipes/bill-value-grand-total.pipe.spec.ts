import { BillValueGrandTotalPipe } from './bill-value-grand-total.pipe';

describe('BillValueGrandTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new BillValueGrandTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
