import { PayValueGrandTotalPipe } from './pay-value-grand-total.pipe';

describe('PayValueGrandTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new PayValueGrandTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
