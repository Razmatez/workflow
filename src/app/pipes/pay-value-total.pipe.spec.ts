import { PayValueTotalPipe } from './pay-value-total.pipe';

describe('PayValueTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new PayValueTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
