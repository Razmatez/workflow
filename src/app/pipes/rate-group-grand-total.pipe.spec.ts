import { RateGroupGrandTotalPipe } from './rate-group-grand-total.pipe';

describe('RateGroupGrandTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new RateGroupGrandTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
