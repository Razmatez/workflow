import { BillValueTotalPipe } from './bill-value-total.pipe';

describe('BillValueTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new BillValueTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
