import { RateGroupTotalPipe } from './rate-group-total.pipe';

describe('RateGroupTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new RateGroupTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
